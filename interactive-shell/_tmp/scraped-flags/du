--files0-from=	
--null	end each output line with NUL, not newline
--all	write counts for all files, not just directories
--apparent-size	print apparent sizes, rather than disk usage; although
--block-size=	scale sizes by SIZE before printing them; e.g.,
--apparent-size	--block-size=1'
--total	produce a grand total
--dereference-args	dereference only symlinks that are listed on the
--max-depth=	print the total for a directory (or file, with --all)
--max-depth=	is the same as
--summarize	
--files0-from=	summarize disk usage of the
--dereference-args	(-D)
--human-readable	print sizes in human readable format (e.g., 1K 234M 2G)
--inodes	list inode usage information instead of block usage
--block-size=	
--dereference	dereference all symbolic links
--count-links	count sizes many times if hard linked
--block-size=	
--no-dereference	don't follow any symbolic links (this is the default)
--separate-dirs	for directories do not include size of subdirectories
--si	like -h, but use powers of 1000 not 1024
--summarize	display only a total for each argument
--threshold=	exclude entries smaller than SIZE if positive,
--time	show time of the last modification of any file in the
--time=	show time as WORD instead of modification time:
--time-style=	show times using STYLE, which can be:
--exclude-from=	exclude files that match any pattern in FILE
--exclude=	exclude files that match PATTERN
--one-file-system	skip directories on different file systems
--help	display this help and exit
--version	output version information and exit
